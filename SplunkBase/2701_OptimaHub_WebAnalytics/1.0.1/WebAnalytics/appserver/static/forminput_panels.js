require(['jquery'], function($){
	$(function() {

		// When the DOM is ready we move the input elements into the panels
		// where we want them to be displayed

//		$('#field1').prependTo($('#single1')).find('label').hide();
//		$('#field2').prependTo($('#single2')).find('label').hide();
//		$('#field3').prependTo($('#table1 .panel-body')).find('label').hide();
        $('#field4').prependTo($('#visitorchart1 .panel-body')).find('label').hide();
		
	});
});
