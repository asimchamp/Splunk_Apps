[module]
className = Splunk.Module.PageRefresh
superClass = Splunk.Module
description = This module sets a refresh function to fire periodically. If no menus are open when the function fires, the page is reloaded.

[param:timeoutInSeconds]
required = True
label = This parameters defines the timeout in seconds after which a page reload should be attempted.