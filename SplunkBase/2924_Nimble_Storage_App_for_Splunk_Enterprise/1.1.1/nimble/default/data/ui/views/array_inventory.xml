<dashboard>
  <label>Arrays</label>
  <row>
    <panel>
      <table>
        <search>
          <query>sourcetype=nimble_rest source="nimble:rest:arrays_detail" | stats latest(serial) as Serial, latest(model) as Model, latest(pool_name) as Pool, latest(version) as Version by name | rename name as Name</query>
          <earliest>-4h@m</earliest>
          <latest>now</latest>
        </search>
      </table>
    </panel>
  </row>
</dashboard>