<form>
  <label>Data Model Search</label>
  <description>Runs a data model search for the selected data model and object.</description>
  <fieldset autoRun="true" submitButton="false">
    <input type="dropdown" token="datamodel">
      <populatingSearch fieldForValue="Title" fieldForLabel="Name">
        <![CDATA[| rest /services/datamodel/model | dedup displayName title | table displayName title | rename displayName as Name, title AS Title]]>
      </populatingSearch>
    </input>
    <input type="dropdown" token="object">
      <populatingSearch fieldForValue="object" fieldForLabel="object">
        <![CDATA[| rest /services/datamodel/model/$datamodel$ | spath input=description path=objectNameList{} output=objectList | fields objectList | mvexpand objectList | rename objectList AS object ]]>
      </populatingSearch>
    </input>
    <input type="time" searchWhenChanged="true">
      <default>
        <earliestTime>-15m</earliestTime>
        <latestTime>now</latestTime>
      </default>
    </input>
  </fieldset>
  <row>
    <table>
      <searchTemplate>| datamodel $datamodel$ $object$ search | table _time,host,sourcetype, _raw *</searchTemplate>
    </table>
  </row>
</form>